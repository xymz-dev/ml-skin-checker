# Architecture

Clean Architecture + Provider Pattern

Frontend - Next.js 15 App Router
Backend - FastAPI Clean: routers -> services -> repositories -> providers + models

MockProvider is default for legal safety.
